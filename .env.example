# Copy this file to .env and fill in real values. Never commit .env.

# Multimodal LLM API key, used by the form -> schema engine (Phase 1+)
# and as the cloud fallback for answer parsing / voice.
EXPO_PUBLIC_LLM_API_KEY=
